{
  "name": "qdchrome-extension",
  "version": "0.2.0",
  "private": true,
  "description": "qdistro native-messaging WebExtension (MV3 + MV2).",
  "scripts": {
    "build": "bash scripts/build-extension.sh",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^29.1.1",
    "vitest": "^1.6.0"
  }
}
